## Agent skills

### Issue tracker

Issues and specs live as GitHub issues in this repo (`LesEntrecodeurs/teamrealestate`), driven by the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Default canonical triage label vocabulary — `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context layout — `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.

### Mantly

Documented by the Mantly project `LesEntrecodeurs/teamrealestate`, pinned in `.mantly/project.json`. See `docs/agents/mantly.md`.
